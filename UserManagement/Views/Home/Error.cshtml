@{
    ViewBag.Title = "500 - Error Page";
    Layout = "~/Views/Shared/_AccountLayout.cshtml";
}

<style>
    .not-found-wrapper {
        @* display: block; *@
        height: 100vh;
        width: 100vw;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 80vh;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 40px 20px;
        border-radius: 8px;
    }

    .not-found-content {
        max-width: 600px;
        margin: auto;
        background-color: white;
        padding: 40px;
        border-radius: 12px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.05);
    }

    .not-found-content h1 {
        font-size: 72px;
        font-weight: bold;
        color: #dc3545;
    }

    .not-found-content p {
        font-size: 18px;
        margin-bottom: 30px;
        color: #6c757d;
    }

    .not-found-content a.btn {
        padding: 10px 30px;
        font-size: 16px;
        border-radius: 5px;
    }
</style>

<div class="not-found-wrapper">
    <div class="not-found-content">
        <h1>500</h1>
        <p>Oops! Something went wrong.</p>
        <a href="@Url.Action("Index", "Dashboard")" class="btn btn-primary">Go to Dashboard Page</a>
    </div>
</div>